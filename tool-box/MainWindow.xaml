<Window x:Class="tool_cantam.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tool Box" Width="1100" Height="720" MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen" Background="Black">

    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9A9AA0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root" Background="{TemplateBinding Background}" CornerRadius="8">
                            <Grid>
                                <Border x:Name="accent" Width="3" Background="#00FF9C" HorizontalAlignment="Left" Opacity="0"/>
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#14141A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="accent" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#00FF9C"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="accent" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#090909"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderBrush" Value="#1F1F24"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#090909">
            <StackPanel>
                <TextBlock Text="TOOL BOX" Foreground="#00FF9C" FontSize="18" FontWeight="Bold" Margin="25,35,0,20"/>
                <TextBlock Text="MODULLER" Foreground="#666" FontSize="10" Margin="25,0,0,10"/>

                <StackPanel Margin="0,10">
                    <Button x:Name="BtnPassword" Style="{StaticResource NavButton}" Content="Parola Yoneticisi" Height="43"/>
                    <Button x:Name="BtnBase64" Style="{StaticResource NavButton}" Content="Base64 Araci" Height="43"/>
                    <Button x:Name="BtnMetadata" Style="{StaticResource NavButton}" Content="Veri Temizleyici" Height="43"/>
                    <Button x:Name="BtnHash" Style="{StaticResource NavButton}" Content="Hash Uretici" Height="43"/>
                    <Button x:Name="BtnPort" Style="{StaticResource NavButton}" Content="Port Tarayici" Height="43"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="Panel" Foreground="White" FontSize="28" FontWeight="SemiBold"/>
                <TextBlock Text="Sistem genel bakis ve moduller" Foreground="#777" FontSize="12"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="1">

                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Aktif Araclar" Foreground="#666" FontSize="10"/>
                        <TextBlock Text="05 Modul" Foreground="#00FF9C" FontSize="24"/>
                    </StackPanel>
                </Border>

            </UniformGrid>

            <Border Grid.Row="2" Margin="5,15,5,5" Background="#090909" CornerRadius="12" BorderBrush="#1F1F24" BorderThickness="1">
                <ContentControl x:Name="MainContentFrame">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Tool Box" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Soldan bir modul secin" Foreground="#777" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </ContentControl>
            </Border>
        </Grid>
    </Grid>
</Window>